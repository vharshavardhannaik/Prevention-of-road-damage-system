# 🏗️ Admin Portal Architecture & Flow

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     SMART ROAD SYSTEM                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              FRONTEND (React)                        │  │
│  │           http://localhost:3000                      │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │                                                      │  │
│  │  ┌──────────────────┐      ┌──────────────────┐    │  │
│  │  │   User App       │      │  Admin Portal    │    │  │
│  │  ├──────────────────┤      ├──────────────────┤    │  │
│  │  │ • Home           │      │ • Login Page     │    │  │
│  │  │ • Report Damage  │◄──┐  │ • Dashboard      │    │  │
│  │  │ • View Roads     │   │  │ • Add Road       │    │  │
│  │  │ • Dashboard      │   │  │ • Edit Road      │    │  │
│  │  │ • File Complaint │   │  │ • Delete Road    │    │  │
│  │  └──────────────────┘   │  │ • Manage Admins  │    │  │
│  │                         │  └──────────────────┘    │  │
│  │  Roads added by admins──┘                          │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │ HTTP Requests                   │
│                           ▼                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           BACKEND API (Node.js/Express)             │  │
│  │          http://localhost:5000/api                  │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │                                                      │  │
│  │  ┌─────────────────────────────────────────────┐   │  │
│  │  │         Admin Routes (/api/admin)           │   │  │
│  │  ├─────────────────────────────────────────────┤   │  │
│  │  │ • POST   /login                             │   │  │
│  │  │ • POST   /register (super_admin only)       │   │  │
│  │  │ • GET    /profile                           │   │  │
│  │  │ • POST   /roads (Add)                       │   │  │
│  │  │ • GET    /roads (List)                      │   │  │
│  │  │ • PUT    /roads/:id (Edit)                  │   │  │
│  │  │ • DELETE /roads/:id (Delete)                │   │  │
│  │  └─────────────────────────────────────────────┘   │  │
│  │                                                      │  │
│  │  ┌─────────────────────────────────────────────┐   │  │
│  │  │     Middleware (Authentication)              │   │  │
│  │  ├─────────────────────────────────────────────┤   │  │
│  │  │ • JWT Token Verification                    │   │  │
│  │  │ • Role-Based Access Control                 │   │  │
│  │  └─────────────────────────────────────────────┘   │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │ Data Queries                    │
│                           ▼                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │            DATABASE (MySQL/SQLite)                  │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │                                                      │  │
│  │  ┌──────────────────┐      ┌──────────────────┐    │  │
│  │  │   Admin Table    │      │  RoadProject     │    │  │
│  │  ├──────────────────┤      │  Table           │    │  │
│  │  │ • id             │      ├──────────────────┤    │  │
│  │  │ • username       │      │ • id             │    │  │
│  │  │ • email          │      │ • roadId         │    │  │
│  │  │ • password       │      │ • roadName       │    │  │
│  │  │ • fullName       │      │ • latitude       │    │  │
│  │  │ • role           │      │ • longitude      │    │  │
│  │  │ • isActive       │      │ • address        │    │  │
│  │  │ • timestamps     │      │ • contractorId   │    │  │
│  │  └──────────────────┘      │ • status         │    │  │
│  │                            └──────────────────┘    │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## User Journey: Admin Adding a Road

```
┌────────────────────────────────────────────────────────────────┐
│ 1. ADMIN OPENS PORTAL                                          │
└────────────────────┬───────────────────────────────────────────┘
                     ▼
             [http://localhost:3000/admin/login]
                     │
                     ├─ Enter username: admin
                     ├─ Enter password: Admin@456
                     └─ Click LOGIN
                     
┌────────────────────────────────────────────────────────────────┐
│ 2. AUTHENTICATION                                              │
└────────────────────┬───────────────────────────────────────────┘
                     ▼
        POST /api/admin/login
            ↓         ↓         ↓
        ┌───┴────────┴───┐
        │ Verify Creds   │
        │ in Database    │
        └───┬────────────┘
            │
            ├─ Match found → Generate JWT Token
            │
            └─ Token stored in localStorage
            
┌────────────────────────────────────────────────────────────────┐
│ 3. DASHBOARD LOADS                                             │
└────────────────────┬───────────────────────────────────────────┘
                     ▼
       [http://localhost:3000/admin/dashboard]
       (Protected Route - checks localStorage token)
            │
            ├─ Get token from localStorage
            ├─ Validate token with backend
            └─ Load roads list
            
┌────────────────────────────────────────────────────────────────┐
│ 4. ADMIN ADDS NEW ROAD                                         │
└────────────────────┬───────────────────────────────────────────┘
                     ▼
         Click "Add New Road" Button
                     │
                     ├─ Road ID: ROAD-001
                     ├─ Road Name: Highway to Delhi
                     ├─ Address: Downtown Area
                     └─ Click "Add Road"
                     
┌────────────────────────────────────────────────────────────────┐
│ 5. SAVE TO DATABASE                                            │
└────────────────────┬───────────────────────────────────────────┘
                     ▼
         POST /api/admin/roads
         {
           roadId: "ROAD-001",
           roadName: "Highway to Delhi",
           address: "Downtown Area"
         }
         + Authorization Header (JWT Token)
            │
            ├─ Server verifies token
            ├─ Validates Road ID is unique
            ├─ Inserts into RoadProject table
            └─ Returns success response
            
┌────────────────────────────────────────────────────────────────┐
│ 6. ROAD NOW AVAILABLE TO USERS                                 │
└────────────────────┬───────────────────────────────────────────┘
                     ▼
         User opens [http://localhost:3000]
         Clicks "Report Damage" 🚨
         Enters Road ID: ROAD-001
            │
            ├─ System searches database
            ├─ Finds ROAD-001 (added by admin)
            ├─ Shows road details
            └─ User can file complaint
            
```

---

## Admin Portal Data Flow

```
┌─────────────┐
│  Browser    │
│  Storage    │
│ (localStorage)│
│  - Token    │
│  - Username │
└──────┬──────┘
       │ Read/Write
       │
┌──────▼──────────────────────────────┐
│         Admin Component              │
│  ┌──────────────────────────────┐   │
│  │ AdminLogin.jsx               │   │
│  │ • Form submission            │   │
│  │ • Error handling             │   │ ──POST /api/admin/login──►
│  └──────────────────────────────┘   │
│  ┌──────────────────────────────┐   │
│  │ AdminDashboard.jsx           │   │
│  │ • List roads                 │   │
│  │ • Add/Edit/Delete forms      │   │ ──GET /api/admin/roads───►
│  │ • Success/Error messages     │   │ ──POST /api/admin/roads──►
│  │ • User feedback              │   │ ──PUT /api/admin/roads/{id}
│  └──────────────────────────────┘   │ ──DELETE /api/admin/roads/{id}
└─────────────────────────────────────┘


                    HTTP Requests
                  (with JWT Token)
                        ▼

┌─────────────────────────────────────┐
│     Backend API (Node.js)           │
│  ┌─────────────────────────────┐   │
│  │ Middleware (auth.js)        │   │
│  │ • Verify JWT Token          │   │
│  │ • Check Role                │   │
│  └──────┬──────────────────────┘   │
│         │                           │
│  ┌──────▼──────────────────────┐   │
│  │ Routes (admin.js)           │   │
│  │ • /login                    │   │
│  │ • /roads (GET/POST/PUT)     │   │
│  │ • /register                 │   │
│  │ • /profile                  │   │
│  └──────┬──────────────────────┘   │
│         │                           │
│  ┌──────▼──────────────────────┐   │
│  │ Models (Admin, Road)        │   │
│  │ • Database queries          │   │
│  │ • Validation                │   │
│  └──────┬──────────────────────┘   │
└─────────┼──────────────────────────┘
          │
          ▼
┌─────────────────────────────────────┐
│        Database                     │
│  ┌──────────────────────────────┐   │
│  │ Admin Table                  │   │
│  │ (Stores admin users)         │   │
│  └──────────────────────────────┘   │
│  ┌──────────────────────────────┐   │
│  │ RoadProject Table            │   │
│  │ (Stores roads added by admin)│   │
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

## Authentication Flow

```
ADMIN LOGIN PROCESS:

1. Admin enters credentials
   ├─ Username: admin
   └─ Password: Admin@456
   
2. Frontend sends to backend
   └─ POST /api/admin/login
   
3. Backend processes
   ├─ Find admin by username
   ├─ Compare password (bcrypt)
   ├─ If valid:
   │  └─ Generate JWT Token
   ├─ If invalid:
   │  └─ Return 401 error
   └─ Send response with token
   
4. Frontend receives token
   ├─ Verify success status
   ├─ Store token in localStorage
   ├─ Store username in localStorage
   └─ Redirect to dashboard
   
5. Dashboard requests
   ├─ Read token from localStorage
   ├─ Add to Authorization header
   ├─ Send GET /api/admin/roads
   ├─ Backend verifies token
   └─ Return roads data


TOKEN STRUCTURE (JWT):
┌─────────────────────────────────────┐
│ Header     │ Payload    │ Signature │
├────────────┼────────────┼───────────┤
│ alg: HS256 │ id: 1      │ Secret    │
│ type: JWT  │ username   │ Key       │
│            │ email      │           │
│            │ role: admin│           │
│            │ exp: 24h   │           │
└─────────────────────────────────────┘
```

---

## Road Management Workflow

```
ADMIN ADDS ROAD:

Form Submission
   └─ {roadId, roadName, address...}
   
Frontend Validation
   ├─ Check required fields
   └─ Show loading indicator
   
API Request
   ├─ POST /api/admin/roads
   ├─ Include JWT token
   └─ Send form data
   
Backend Processing
   ├─ Verify JWT token
   ├─ Validate role (admin)
   ├─ Check Road ID uniqueness
   ├─ Insert into database
   └─ Return created road
   
Frontend Response
   ├─ Show success message
   ├─ Clear form
   ├─ Refresh roads list
   └─ Display new road in table


ADMIN EDITS ROAD:

Click Edit Button
   └─ Populate form with current data
   
User Updates Fields
   └─ {roadName, address...}
   
API Request
   ├─ PUT /api/admin/roads/{id}
   ├─ Include JWT token
   └─ Send updated data
   
Backend Processing
   ├─ Verify JWT token
   ├─ Find road by ID
   ├─ Update fields
   ├─ Save to database
   └─ Return updated road
   
Frontend Response
   ├─ Show success message
   ├─ Close form
   ├─ Refresh roads list
   └─ Display updated road


ADMIN DELETES ROAD:

Click Delete Button
   ├─ Show confirmation dialog
   └─ "Are you sure?"
   
User Confirms
   └─ Proceed with deletion
   
API Request
   ├─ DELETE /api/admin/roads/{id}
   ├─ Include JWT token
   └─ Send request
   
Backend Processing
   ├─ Verify JWT token
   ├─ Find road by ID
   ├─ Check for linked complaints
   ├─ Delete road record
   └─ Return success response
   
Frontend Response
   ├─ Show success message
   ├─ Refresh roads list
   └─ Road no longer visible
```

---

## User App Integration

```
HOW USER APP DISPLAYS ADMIN-ADDED ROADS:

Admin adds ROAD-001 in Admin Portal
   └─ Saved in Database
   
User opens User App
   ├─ http://localhost:3000
   └─ Goes to home page
   
User clicks "Report Damage"
   ├─ Navigates to complaint page
   └─ See "Enter Road ID" form
   
User enters: ROAD-001
   ├─ Clicks "Find Road"
   └─ Frontend sends request
   
Backend lookup
   ├─ GET /api/roads/ROAD-001
   ├─ Searches RoadProject table
   ├─ Finds road added by admin
   └─ Returns road data
   
Frontend displays
   ├─ Road ID: ROAD-001
   ├─ Road Name: Highway to Delhi
   ├─ Address: Downtown Area
   └─ Shows complaint form
   
User files complaint
   ├─ Takes photo
   ├─ Describes damage
   ├─ Selects severity
   └─ Submits complaint
   
Complaint saved
   ├─ Linked to Road: ROAD-001
   ├─ Stored in database
   └─ Available in admin dashboard
```

---

## Security Layers

```
LAYER 1: FRONTEND PROTECTION
┌─────────────────────────────────────┐
│ ProtectedAdminRoute Component       │
│ ├─ Check localStorage for token    │
│ ├─ If no token → Redirect to login │
│ └─ If token exists → Allow access  │
└─────────────────────────────────────┘

LAYER 2: API AUTHENTICATION
┌─────────────────────────────────────┐
│ verifyAdminToken Middleware         │
│ ├─ Extract token from header        │
│ ├─ Verify JWT signature             │
│ ├─ Check token expiration           │
│ ├─ If invalid → Return 401          │
│ └─ If valid → Continue to route     │
└─────────────────────────────────────┘

LAYER 3: ROLE-BASED ACCESS
┌─────────────────────────────────────┐
│ Role Verification                   │
│ ├─ Check decoded.role               │
│ ├─ If admin/super_admin → Allow    │
│ ├─ If user role → Deny             │
│ └─ Return 403 Forbidden            │
└─────────────────────────────────────┘

LAYER 4: DATABASE VALIDATION
┌─────────────────────────────────────┐
│ Model Constraints                   │
│ ├─ Unique indexes on roadId         │
│ ├─ NOT NULL constraints             │
│ ├─ Foreign key checks               │
│ └─ Data type validation             │
└─────────────────────────────────────┘

LAYER 5: PASSWORD SECURITY
┌─────────────────────────────────────┐
│ bcryptjs Password Hashing           │
│ ├─ Salt rounds: 10                  │
│ ├─ Hash on create                   │
│ ├─ Hash on password update          │
│ └─ Compare safely on login          │
└─────────────────────────────────────┘
```

---

## Component Structure

```
Admin Portal UI Structure:

/admin/login
    ↓
AdminLogin.jsx
├─ Header (Logo & Title)
├─ Login Form
│  ├─ Username Input
│  ├─ Password Input
│  └─ Login Button
├─ Error Display
├─ Demo Credentials
└─ Footer

    ↓ (After Login)

/admin/dashboard
    ↓
AdminDashboard.jsx
├─ Header (Navigation)
│  ├─ Title: "Admin Dashboard"
│  └─ Logout Button
├─ Alert Messages
│  ├─ Error Alerts
│  └─ Success Alerts
├─ Add Road Button
├─ Add/Edit Road Form (Conditional)
│  ├─ Road ID Input
│  ├─ Road Name Input
│  ├─ Latitude Input
│  ├─ Longitude Input
│  ├─ Address Input
│  ├─ Submit Button
│  └─ Cancel Button
└─ Roads Table
   ├─ Table Header
   │  ├─ Road ID
   │  ├─ Road Name
   │  ├─ Address
   │  ├─ Status
   │  └─ Actions
   └─ Table Rows
      └─ Road data with Edit/Delete buttons
```

---

This architecture ensures:
- ✅ Secure authentication and authorization
- ✅ Separation between admin and user concerns
- ✅ Data integrity and validation
- ✅ Responsive and user-friendly interface
- ✅ Scalable for future enhancements
